# Essay imagery storyboards v1

External image-generation handoff; not an implementation request. Read `00-style-reference.md` first. Generate required heroes before figures and preserve all **EXACT ON-IMAGE TEXT** literally. Do not add claims, logos, robots, people, simulated product UI, or unreadable decorative copy.

For each approved board return an editable source, master SVG (diagram) or PNG (hero), 2× raster export, generation metadata, and contact-sheet proof at intended article width. `single` needs one approved render; `dark+light pair` needs composition-matched scheme outputs. The manifest gives the intended target path. Any board marked **optional net-new** is an additive proposal, not a prerequisite for publishing.
