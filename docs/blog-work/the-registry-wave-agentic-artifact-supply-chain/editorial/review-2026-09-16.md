# Editorial review — Registry Wave, September 2026 edition (draft 1)

Audits `src/content/posts/the-registry-wave-agentic-artifact-supply-chain.mdx` at commit `7f0620d`
(Astra's full rewrite) against `.leg/field-notes/rw-writer-astra.md` (the commissioning brief and
verdicts) and `.leg/field-notes/feedback-ledger.md` (the row table). Read-only audit; no edits made
to the draft. Line numbers below refer to the draft file as currently committed.

## 1. Ledger conformance table

| id | verdict | status | evidence (draft lines) |
|---|---|---|---|
| G1 | ACCEPT (merged w/ M1) | **landed** | 84-99, single Wave1/Wave2 x four-assurances table (88-93) |
| G2 | ACCEPT | **landed** | 5 (frontmatter `updatedDate`), 37-39 (edition Callout) |
| G3 | ACCEPT | **landed** | 108 ("The runtime executes. A skill shapes its behavior..."); confirmed zero remaining instances of "execute, reason, and act on their own" anywhere in the file |
| G4 | ACCEPT (reconciled w/ G9) | **landed** | 161-176 ("A control fabric across many systems"), 222-223 (Operator framed as one implementation, not a requirement) |
| G5 | ACCEPT | **landed** | 194-204, points forward to *The Deterministic Envelope* |
| G6 | ACCEPT | **landed** | 41-58, full opening rewrite: concrete example → "What exactly did the approval attach to?" (47) → thesis line (49) → shortened SkillMeat origin story (53-57) |
| G7 | ACCEPT (w/ M2) | **landed** | AOSBench/SystemCards/ablation paragraph removed entirely from body; zero matches for "AOSBench", "SystemCards", "ablation" anywhere in the file (verified by grep) — writer went further than the brief's "at most one sentence" floor and cut it outright |
| G8 | ACCEPT | **landed** | SkillMeat is the only internally-named implementation carried in the main body; Operator/MeatyWiki/Research Foundry/MeatyPrompts are confined to the single "What this looks like in my lab" Callout (222-230). Minor note: "AOS" itself is named once in body prose (176), with a one-clause intro ("my personal lab system") — defensible since it's naming the acronym rendered inside the interactive component the sentence is describing, not a second unrelated system, but it is a second internal name in body prose beyond SkillMeat |
| G9 | ACCEPT (w/ G4) | **landed** | 165-171, four named responsibilities table (supply chain / runtime control / orchestration / evidence) replaces monolithic "control plane" as the destination concept |
| G10 | ACCEPT | **landed** | 125-159, full lifecycle: authoring → registration → approval → deployment (Figure 01) → use → drift → detection (Figure 02) → missing-dependency/provisioning → local-first ergonomics (M3) → replacement/revocation. Note: brief's own Sustained-Example spec (field notes) supersedes the ledger's original `database-migration-reviewer` suggestion — the writer correctly followed the brief, not the raw ledger text |
| G11 | ACCEPT (placeholder) | **landed** | 3 `<Figure>` blocks (131-136, 145-150, 214-219), each with an exact capture-spec HTML comment and an Implemented/Proposed label in the caption |
| G12 | ACCEPT | **landed** | 245-248, "Where this sits" Callout with prior/this/next/destination + umbrella-term line |
| G13 | ACCEPT (delegated) | **landed** | 73-80, 4 footnoted vendor claims ([^github], [^jfrog], [^redhat], [^mcp]), each paired with a `(to verify)` HTML comment |
| G14 | DEFER (≤3,200 body words, no padding) | **landed anyway** | Body word count (frontmatter/imports/comments stripped) is ~2,883 words, down from ~3,068 in the pre-refresh `main` body — the essay actually *shrank* while adding two interactives and three figure placeholders. `readTime` moved 15→14 min, consistent with the drop |
| G15 | program | **landed** | 248, "I call the umbrella Agentic Systems Engineering. Agentic SDLC is its application to software delivery." |
| G16 | program | **landed** | 59-63, six-field "The question this essay carries" Callout (prior question / this thesis / next question / built and tested in / related external work / status) |
| G17 | program | **landed** | Evidence-class tags used inline (`[Observed]` 41, 184; `[Externally reported]` 73; `[Proposed synthesis]` 86, 186) plus a legend Callout (241-243) that explicitly keeps Observed and Measured distinct and states no measured outcome is claimed |
| G18 | program | **landed** | 232-239, 4-item receipt rail (drift / parity / staleness / provisioning) — within the 2-4 range |
| G19 | program | **N/A / satisfied by omission** | AOSBench is not named anywhere in the essay (see G7), so it's not "just mentioned as a noun" — it's absent, which is the safer reading of G19's intent at this stage |
| G20 | program | **landed** | 248, "This reading path connects the questions; it isn't a one-to-one map of architecture layers." |
| G21 | program | **landed** | Consistent "reference implementation, not the thesis" framing throughout (176, 222-223) |
| G22 | program | **landed** | No inevitability phrasing found (confirmed by grep for "will have to", "must adopt", "inevitab*", "will not accept" — zero hits). "an enterprise will run many governance systems" (163) is descriptive, not a mandate |
| G23 | program | **landed** | 206-220, dated September 11, 2026 instance |
| G24 | program | **landed** | No first-use/novelty claims found (grep for "coined", "first to", "invent*" — zero hits) |
| G25 | program | **landed** | `date` kept at 2026-07-01 (original publish); `updatedDate` set to 2026-09-15T21:30 (14 min before the actual commit timestamp, same day — immaterial, not a backdating pattern) |
| M1 | ACCEPT (merged w/ G1) | **landed** | Same table as G1 — resolves the ledger's own flagged G1/M1 redundancy risk as one artifact, per the front's own recommendation |
| M2 | ACCEPT | **landed** | Moot — the paragraph M2 flagged for placement no longer exists (see G7) |
| M3 | ACCEPT | **landed** | 157, Git/CI local-first-ergonomics paragraph |
| M4 | ACCEPT | **landed** | 182-192, "From deployed artifact to delivered outcome" section, Observed/Proposed-synthesis labeled |
| M5 | ACCEPT | **landed** | Confirmed "experiment" spelled correctly at 157; zero matches for "expirement" |
| M6 | ACCEPT | **landed** | 110-119 (taxonomy table) + explicit paragraph "Both executable code and non-executable context are versioned dependencies. Changing either can change runtime behavior." (119); `ArtifactTaxonomy.tsx` mirrors the same 6-row taxonomy |

**Bucket counts:** landed 30, landed-anyway (deferred verdict met) 1 (G14), N/A/satisfied-by-omission 1 (G19), partially 0, missing 0. Every lettered/numbered row in the ledger has a disposition; nothing is unaddressed. The one open item that doesn't map to a ledger row — Term-wrapping "agentic artifact" (structure item 5) — is covered in §2 below.

## 2. Structure check (12 movements)

1. Edition callout (G2) — **present**, 37-39.
2. Opening: example → "what did approval attach to?" → thesis — **present**, 41-49.
3. Wave 1: what registries solved — **present**, "## What the registry wave solved" (65-82).
4. Why not enough: four assurances, Wave1/2 table — **present**, "## Four assurances, four places to lose the thread" (84-99).
5. What an agentic artifact is (Term), taxonomy incl. M6, interactive — **present but partially landed**. The taxonomy table/component and M6 land fully (101-123). The `<Term>` wrap does not: line 105 carries an explicit editor comment ("Editor: add agentic-artifact to the glossary, then wrap the definition label in the native Term component. The current glossary has no matching entry.") and line 106 just bolds **agentic artifact** instead. This is the correct call given `Term.astro` throws a build error on an unresolved `id` (confirmed by reading the component) — wrapping now would have broken the build. Flagged as a "needs the writer" item below.
6. The supply chain: lifecycle + local-first ergonomics (M3) — **present**, "## Follow the reviewer out of the registry" (125-159).
7. The control fabric (G4/G9), interactive re-captioned — **present**, "## A control fabric across many systems" (161-180).
8. Evidence and outcomes (M4) — **present**, "## From deployed artifact to delivered outcome" (182-192).
9. Four non-equivalences callout (G5) — **present**, 194-204.
10. Where the system lied to us — **present**, 206-220.
11. What this looks like in my lab (single callout) — **present**, 222-230.
12. Where this sits + receipt rail + sources — **present**: receipts (232-239), evidence-labels legend (241-243), "Where this sits" (245-248), closing paragraph (251), "## Sources" footnotes (253-266). Ordering differs slightly from the brief's literal list order (receipts precede "Where this sits" rather than follow it) but the brief only said "adapt, do not template" — not a defect.

**12/12 movements present.** Only sub-item 5's Term-wrap is a genuine partial (deferred correctly, not dropped).

## 3. Voice checklist (all 15 items)

1. **Zero em-dashes?** Confirmed by grep (`—`/`–`) — zero hits. **PASS.**
2. **Any sentence carrying 2+ claims that could split?** Mostly clean; the essay is built from short declaratives throughout (e.g., 41-45). One residual candidate: line 108, "Markdown instructions don't reason on their own, and a signed context pack doesn't enforce the permissions it describes" — a parallel-clause "and" construction rather than the short-declarative pattern. Minor, arguably intentional parallelism. **PASS with one minor candidate.**
3. **Any flat claim that could land with "Rather," "But," or a fragment?** Voice rules call this Nick's most load-bearing connective move (4+ instances in the pre-refresh baseline). The new draft has only 2 instances (97, 99, 163) — one of them ("Thus the enterprise question isn't...Rather, the question is...") is Nick's own surviving sentence, preserved verbatim (good — see §4). But the connective is thinner than baseline density elsewhere in the new material. Line 95, "Fixing that closes one gap. A correctly deployed reviewer could still receive stale project context..." reads flat where a "But" would land the contrast harder. **NEEDS FIXING** (listed below).
4. **Any hedge stacked on a hedge?** None found (grep for "not something I can", "honestly claim" — zero hits). **PASS.**
5. **Any formal-definition register that could ground in a concrete example?** Line 103 already converts to direct address ("Give it a concrete shape for a second..."), correctly matching the rule. But line 106 immediately reverts: "An agentic artifact is any reusable, versionable unit that shapes how an agent reasons, acts, accesses tools, applies context, follows policy, or produces work" — this is the exact "X is any Y that..." register the hunks (§7) show Nick cutting. **NEEDS FIXING** (listed below).
6. **Any un-contracted formal phrasing in body prose?** "is not"/"are not" appear at 174 and 194, both inside deliberate aphoristic parallel constructions ("A configured integration is not enforcement; an advisory registration is not a gate" / the "Four things that are not equivalent" Callout title). These read as intentional maxim-style emphasis, consistent with Nick's own uncontracted aphorisms elsewhere (e.g., his memory files' "Registered is not the same claim as enforced"). **PASS, likely intentional — not flagged as a fix.**
7. **Is SkillMeat carrying every example when the claim is general?** No — the lab Callout explicitly widens to MeatyWiki/Research Foundry/MeatyPrompts/Operator (222-225), directly matching the rule. **PASS.**
8. **Any "when I started X" missing a timeline anchor?** Line 54, "It was mid-late 2025 when I started building SkillMeat..." — anchor present. **PASS.**
9. **Any raw identifier in body prose instead of a footnote?** None in body prose (confirmed by grep for node/itt/run/ULID/PR-number patterns — zero body hits). Raw git revision hashes do appear, but only in footnotes (261-265) — correct location per this rule, though see §7 (Dual register) for a separate footnote-consistency issue. **PASS** (on this specific item's terms).
10. **Does every number carry a sentence of stakes?** "Five days" (41), "six weeks" (210) are both embedded in narrative stakes, not bare figures. **PASS.**
11. **Is the closing as tight as the opening?** Yes — closing paragraph (251) calls back directly to the opening reviewer story ("The reviewer didn't need a better name or another catalog..."). **PASS.**
12. **Any exact-word repeat across adjacent sentences reading as redundant?** The "question...question" repeat at 97/99 is Nick's own preserved sentence (see item 3 and §4) — not a machine tell, it's provenance. No new unintentional repeats found on a spot check. **PASS.**
13. **Any list that would land better as a visual?** Both former bullet-list/example sections now render as an interactive (`ArtifactTaxonomy`, `ArtifactControlPlane`) or a markdown table (88-93, 110-119) — matches the site's own table-over-bullets convention. **PASS.**
14. **Dual register (exec + engineer)?** Overall yes — mechanism is explained plainly, every internal name gets an intro clause (see §7). **PASS.**
15. **Any self-deprecating aside/personal detail cut that should have stayed?** "The desire to be lazy" (53) survived. The other documented aside — "That distinction is loadbearing (an inside joke for the Claude 5 model-family users)" — did **not** carry over; the passage it was anchored to no longer exists in this form (the thesis is now delivered as the bolded "**Trusted artifact ≠ trusted use**" line at 49 instead). This looks like a structural consequence of the full rewrite rather than an unmotivated cut, but it is a genuine loss of one of only two confirmed personality-beat instances in the voice rules. **Flagged for the writer, not a mechanical fix** (listed below).

**Net: 12 clean passes, 1 pass-with-caveat (item 6), 2 items needing a fix (items 3, 5), 1 item needing a writer decision (item 15).**

## 4. Currency check (vs. `thought-leadership-direction-2026-09.md` and `exploration-brief.md` §4-7)

- **"Agentic Systems Engineering"** — used correctly as the umbrella term (248), matching the direction doc's D2 decision. **One open risk**: the direction doc's own §7 open-decisions list (item 4) says defer *public* commitment to this term until the employment-agreement read is confirmed complete, and no source read this pass confirms that read finished. The essay states the term publicly and unhedged. This is a Nick-only decision, not something this review can resolve — flagged, not fixed.
- **"Control fabric"** and the four named responsibilities (supply chain / runtime control / orchestration / evidence) — matches the ledger's G4/G9-resolved language exactly (163-171). No mismatch.
- **"The four assurances"** — the heading uses "assurances" (84) and the concept is fully built out in the table, but the exact phrase "the four assurances" as a repeatable named term never appears verbatim; it's described, not coined-and-labeled. Minor — not a mismatch with the direction doc, since the direction doc doesn't itself use that exact phrase either, but worth flagging in case a future essay (The Deterministic Envelope) expects to reference "the four assurances" as an established term.
- **Vendor list narrower than assumed elsewhere.** The direction doc's hand-back planning (exploration-brief.md §11) assumes Registry Wave cites "five external-signal vendors ... (GitHub, JFrog, Red Hat, Google, IBM/Microsoft/ServiceNow control-plane moves)". The actual draft cites only 4 (GitHub, JFrog, Red Hat, MCP Registry) — Google/IBM/Microsoft/ServiceNow are absent. Not a defect (the writer brief's G13 verdict only required footnoting whatever claims survive), but the Deep Research verification pass should be scoped to the 4 actually cited, not the broader list other planning docs assume.
- **"Evidence-class labels"** (Observed / Externally reported / Proposed synthesis / Measured) match the direction doc's G17 spec exactly, including the "don't collapse Observed and Measured" instruction (243).
- No other terminology drift found against the direction doc's §1-§3.

## 5. Claims register

| claim | location | footnote | (to verify)? | status |
|---|---|---|---|---|
| GitHub CLI skill discovery/install/publish | 73 | [^github] | yes (74) | externally reported, verification pending |
| JFrog versioned/scanned/signed/access-controlled skills registry | 75 | [^jfrog] | yes (76) | externally reported, verification pending |
| Red Hat catalogs connect discovery + deployment/runtime connectivity | 77 | [^redhat] | yes (78) | externally reported, verification pending |
| Official MCP Registry supplies discovery metadata | 79 | [^mcp] | yes (80) | externally reported, verification pending |
| Reviewer skill deployed 5 days behind source | 41 | [^drift] | no (Observed, own lab) | dated Sept 11, 2026 receipt |
| Sept 10 instruction-parity gate (6 weeks stale doctrine) | 210 | [^parity] | no (Observed) | dated receipt |
| Sept 10 staleness check (superseded model claims) | 210 | [^staleness] | no (Observed) | dated receipt |
| Sept 14 provisioning rule | 155 | [^provisioning] | no (Observed) | dated receipt, identifiers explicitly withheld |
| Sept 10 fleet-wide compliance labeled target state | 227 | [^coverage] | no (Proposed synthesis, self-labeled as target state not fact) | dated receipt |

**First-use/novelty-sounding language:** none found. No instance of "I coined," "first to," "invented," or equivalent. "Control fabric" is introduced with "I use control fabric for..." (163) — a naming choice, not a priority claim. Consistent with G24.

**Every external/market claim has a footnote + `(to verify)` marker; every internal claim is dated and sourced to a footnote.** No unfootnoted external claim found on a full-text scan.

## 6. Provenance check

- **Edition callout honesty (37-39):** Accurate and appropriately hedged — states first publication (July 2026), re-publication date (Sept 15, 2026), names the two real drivers (external reviews + the writer's own deployment failure), and explicitly disclaims that the new argument existed in July ("The distinction below is the September argument, not a claim about what the July essay already said"). This directly satisfies the brief's own caution ("Never imply the new argument existed in July"). **No issues.**
- **Frontmatter dates:** `date: 2026-07-01T12:00:00-04:00` unchanged (correct per brief — original publish date). `updatedDate: 2026-09-15T21:30:00-04:00` — checked against the actual commit timestamp (`7f0620d`, 2026-09-15 21:44:17 -0400): the frontmatter time is 14 minutes *before* the real commit, same calendar day. Immaterial gap, not a backdating pattern. **No issues.**
- **`status: published`** retained, consistent with the essay being a live-URL edition refresh rather than a new draft.
- **`draftNotes`** correctly appends the edition line per the brief's instruction (28).

## 7. Figures

All three `<Figure>` blocks carry an exact, unambiguous capture spec in an adjacent HTML comment, plus an Implemented/Proposed label in the visible caption:

- **Figure 01** (131-136, `diagram-market-wave.svg`): capture spec = "show the real SkillMeat version/deploy view for the reviewer with registered revision and deployed destination state; redact machine paths and identifiers." Labeled Implemented. **Note:** this Figure reuses the exact same asset path as the post's `heroImage` (frontmatter line 16). Not wrong, but worth a deliberate call — either confirm hero and Figure 1 are meant to share art, or point Figure 01 at a distinct placeholder before a capture leg runs.
- **Figure 02** (145-150, `diagram-registry-control-plane.svg`): capture spec = "capture a real manifest/deployment diff showing the reviewer guard present upstream and absent in the deployed copy; label any replay as a reconstruction and redact paths/identifiers." Labeled Proposed. Clear and capturable as written.
- **Figure 03** (214-219, `diagram-intent-outcome-chain.svg`): capture spec = "capture actual drift-gate output identifying a stale reviewer copy and a nonzero exit, then the result after recorded deployment; label replayed evidence and redact paths/identifiers." Labeled Implemented. Clear and capturable as written.

**Unused asset:** `public/assets/posts/the-registry-wave-agentic-artifact-supply-chain/diagram-artifact-taxonomy.svg` exists on disk but is referenced by no `<Figure>`, no `heroImage`, and no `interactiveElement` value in this draft. Likely a leftover from a pre-interactive version of the taxonomy section (which now uses `<ArtifactTaxonomy client:visible />` instead of a static figure). Worth a decision: wire it in or flag for deletion so a future capture leg doesn't treat it as a missing reference.

**Interactives vs. text — both checked and consistent:**
- `ArtifactTaxonomy.tsx` — 6 categories (Instruction, Tool, Context, Governance, Execution, Evidence), identical set and order to the body's taxonomy table (110-119), and its governance-question copy echoes the M6 "both executable and non-executable are versioned dependencies" point. **Matches.**
- `ArtifactControlPlane.tsx` — 6 layers (Registry, Governance, Context, Distribution, Evidence, Adapters). The body prose (176) maps these onto the new four responsibilities only in aggregate prose ("registry and distribution serve the supply chain; governance, context, and adapters meet runtime and orchestration boundaries; evidence connects the result") without saying which of Governance/Context/Adapters maps to runtime control vs. orchestration specifically. Functionally consistent, but the per-layer assignment is fuzzier than the four-responsibility table it's meant to illustrate. Flagged below as a "needs the writer" item, since resolving it means either touching the component (out of scope per the writer brief's own file restriction) or adding more precise prose.

## 8. Dual register + identifiers

- **No raw ids/paths/ULIDs/PR numbers in body prose.** Confirmed clean by grep.
- **Every named subsystem gets a plain-language intro clause on first mention:** SkillMeat (54, "my registry and deployment tool for reusable agent capabilities"), MeatyWiki (224, "my durable knowledge store"), Research Foundry (224, "my evidence-oriented research system"), MeatyPrompts (225, "my earlier prompt-management experiment"), the Operator (222-223, "my work-routing surface"), AOS (176, "my personal lab system"). **Fully compliant.**
- **Footnote identifier inconsistency (the one real gap in this section):** four of five internal-receipt footnotes carry a raw git revision short-hash — `9a430766` (261), `4c1850e8` (262), `cc0fda2e` (263), `78d0aa78` and `885b1b07` (265) — while the fifth (`[^provisioning]`, 264) explicitly states "Internal identifiers withheld; method only." The essay applies two different disclosure policies across otherwise-identical footnote entries. These hashes carry zero explanatory value for a public reader and are the kind of raw identifier the dual-register rule is aimed at, even though they sit in a footnote rather than body prose. Listed as an edit-sized fix below.

## Edit-sized fixes

1. Strip the raw git revision hashes from footnotes 261 (`, revision \`9a430766\``), 262 (`, revision \`4c1850e8\``), 263 (`, revision \`cc0fda2e\``), and 265 (`, revision \`78d0aa78\`` and `(August 24, revision \`885b1b07\`)`) to match `[^provisioning]`'s own stated "internal identifiers withheld" policy.
2. Line 95: open the second sentence with "But" — "Fixing that closes one gap. But a correctly deployed reviewer could still receive stale project context..." — to restore the "Rather/But/Thus" connective density the voice rules treat as load-bearing (currently only 2 instances vs. baseline's 4+).
3. Line 106: convert "An agentic artifact is any reusable, versionable unit that shapes how an agent reasons, acts, accesses tools, applies context, follows policy, or produces work" out of the "X is any Y that..." formal-definition register — it reverts to the exact pattern the previous direct-address sentence (103) had just corrected.
4. Frontmatter line 16 / draft line 133: confirm intentionally, or change, that Figure 01's `src` is identical to `heroImage`'s path (`diagram-market-wave.svg`).
5. Decide the fate of the unused asset `public/assets/posts/the-registry-wave-agentic-artifact-supply-chain/diagram-artifact-taxonomy.svg` (wire it into a `<Figure>` or the interactive, or drop it).

## Needs the writer

1. Add glossary.ts entries for `agentic-artifact` (and optionally `control-fabric`) so line 106 can be wrapped in `<Term id="...">` per Structure item 5 — currently deferred with an explicit in-file editor comment (line 105) because `Term.astro` throws on an unresolved id.
2. Decide whether to reintroduce Nick's "loadbearing (inside joke for the Claude 5 model-family users)" personality beat somewhere in the new structure — it did not survive the full rewrite of the passage it was originally anchored to, and it's one of only two confirmed inside-joke instances the voice rules document.
3. `ArtifactControlPlane.tsx`'s 6 layers map to the essay's 4 new control-fabric responsibilities only in aggregate prose, not per-layer. Tightening this means either touching the component (outside the writer brief's file scope, which permits only `ArtifactTaxonomy*`) or accepting the current fuzziness — a decision, not a text edit.
4. Confirm whether public, unhedged use of "Agentic Systems Engineering" (line 248) is cleared yet — the direction doc's own open-decisions list defers public commitment on this term pending an unconfirmed employment-agreement read.
5. Confirm the Deep Research verification pass should be scoped to the 4 vendors actually cited (GitHub, JFrog, Red Hat, MCP), not the broader vendor list other planning docs assume this essay would name.
