Registry Wave reading-experience v2: fix the atlas capture in _handoff/rw-v2/qa/run_qa.py and recapture. No src/ edits. No image viewing. No git writes. No emdashes. Dev server at http://localhost:4399 (do not start another; do not run npm).

Defects found in the current atlas/final captures (docs/blog-work/the-registry-wave-agentic-artifact-supply-chain/reading-experience-v2/atlas/final/):
1. Thread Focus overlay captured mid fade-in (dialog and essay both visible, translucent). Wait for the dialog's opening transition to finish: wait for `getComputedStyle(dialog).opacity === '1'` and no running animations (`dialog.getAnimations().length === 0`), plus 300ms, before every overlay screenshot (Thread Focus, Evidence Focus, Figure Viewer, mobile Contents sheet).
2. evidence-record used the URL /evidence/drift-check-2026-08-06/ which 404s. Valid ids are in src/data/evidence.ts (e.g. drift-check). Use /essays/the-registry-wave-agentic-artifact-supply-chain/evidence/drift-check/ and also capture the in-essay Evidence Focus overlay opened from the "evidence" link, not only the route.
3. conclusion captured the hero. Scroll so the last section marker ("What I can prove today.") heading sits at the top of the viewport, wait for images (all `img` complete) and 500ms, then capture; also capture the very end (WhereThisSits / related work / sources) as conclusion-end.
4. Add captures: executive-signal region (viewport at y=0 desktop is fine but also a 1440x1400 full-height entry capture named entry-tall), first section marker + first pull quote (first-scroll: scroll so the first h2 section marker is at top), managed-variation figure inline (the "Not every difference is drift" figure: scroll it to center), and the rail alone cropped (clip to the aside bounding box) at the first-scroll position, named rail-companion.
5. Preload: before any capture, scroll to the bottom and back to the top and wait until every img is complete, so lazy images do not shift layout.
6. Dark theme: add class "dark" to <html> before load via an init script, and also set the theme in localStorage if the site reads one (check src/layouts/BaseLayout.astro for the key). Capture desktop 1440x900 dark AND light, mobile 390x844 dark AND light, JPEG quality 85.

Overwrite atlas/final/ (delete the old jpgs first) and rewrite INDEX.md (one line per file). Keep under 8 MB. Final message under 150 words: file count, size, and any state you could not capture (with why).
