Registry Wave reading-experience v2: visual round 3, correctness of round 2 in BOTH themes. Same rules as _handoff/rw-v2/legs/visual-fix-1-brief.txt (read it; no images, no prose/MDX/data edits, no npm, no git writes, no emdashes; Chromium works; dev server http://localhost:4399; QA gate 23 PASS / 0 FAIL via run_qa.test_suite, results to _handoff/rw-v2/qa/results-visual-3.txt). Round 2 is uncommitted in the working tree (`git diff --stat` vs 5855b3f); keep it.

The orchestrator verified round 2 in a browser in both themes at 1440x1400. Findings, all to fix:

1. DARK: the in-body Executive Signal is still visible at xl. Cause: src/components/content/ExecutiveSignal.astro line ~110 `:global(.dark) .executive-signal--split { display: block; }` overrides the xl `display: none` in global.css ~275. Fix so the body copy is hidden at xl in both themes and the dark rule only affects the rail/body variant it was meant for (do not use !important; scope by placement).
2. LIGHT: the rail Executive Signal renders as the two-column "split" grid inside the 266px rail: two squeezed columns ("Why this matters" / "Carry this forward") with one word per line. The rail placement must be a single stacked card in both themes (heading, paragraph, hairline, heading, paragraph), the split grid is for the wide body placement only (below xl, if kept at all). Give the rail placement its own class from PostLayout (e.g. `executive-signal--rail`) instead of relying on theme selectors.
3. LIGHT: the author card at 266px wraps "Nick Miethe" onto two lines at a large size and stacks the About/Portfolio links. Match the dark treatment: 38px avatar, 18px name on one line, 12px bio, links inline.
4. LIGHT: the section marker number tile floats left of the title outside the column and the rail cards use plain white plates with no border hierarchy. Make the light theme a coherent counterpart of the dark treatment: same geometry, same rule/tile, light plate tokens (surface #FFFFFF or #F7F8FB, hairline #E3E8F0, violet accent unchanged). Check every component touched in rounds 1-2 in light: ThreadScene plate, PullQuote, RevisionNote strip, SectionMarker, Reading Companion, Running Example, Thread/Evidence/Figure focus surfaces, WhereThisSits, MobileContents.
5. Theme harness note: the QA/capture harness adds the html class via an init script and the site's own theme script then adds `light`, leaving both `light dark` on <html>. Check src/layouts/BaseLayout.astro for the theme storage key and make _handoff/rw-v2/qa/run_qa.py capture_context set that key so only one class is present (harness fix only; do not change the site's theme script).

Verify each item with a DOM measurement at 1440x900 and 390x844 in both themes (report the numbers), then run the QA suite. Final message under 200 words.
