Registry Wave reading-experience v2: QA package + CLOSEOUT. Read-only on src/ (no code edits); you write only under docs/blog-work/the-registry-wave-agentic-artifact-supply-chain/reading-experience-v2/ and _handoff/rw-v2/qa/.

DO NOT open or view any image file in this session (the gateway rejects image input and kills the run). Screenshots you take are saved for humans, never viewed by you.

Read first: docs/blog-work/the-registry-wave-agentic-artifact-supply-chain/reading-experience-v2/DIRECTION.md (all), ARCHITECTURE.md beside it, atlas/p0bc-review/FIX-LIST.md, and $H/05_IMPLEMENTATION_AND_QA.md test table (N01-N12, A01-A04, C01-C04, V01-V04) plus $H/07_OPEN_ITEMS.md, where $H = /Users/miethe/dev/WIP/agents/theses/registry-wave/reg-wave-review-v4/registry_wave_experience_v2. Commits for this package series: `git log --oneline 6b0fe02..HEAD` (d71555f through 68c0f0d); leg logs in _handoff/rw-v2/legs/*.log.

Environment: a dev server is already running at http://localhost:4399 (Astro dev, Node 22, this worktree; do not start another and do not run npm build/check yourself, your sandbox cannot run Node 22). Python Playwright is installed with chromium (the earlier capture used it: see atlas/p0bc-review/capture-log-2.txt for the pattern). Essay URL: /essays/the-registry-wave-agentic-artifact-supply-chain/ ; routes: .../thread/<beat>/, .../evidence/, /evidence/<id>/ under the essay, .../figure/<id>/. Force dark theme by adding class "dark" to <html> (the site is class-based), light by removing it.

Do:
1. Write _handoff/rw-v2/qa/run_qa.py (Playwright, headless chromium) that drives each test in the $H/05 table that can be driven in a browser: N01-N12 (focus/return semantics, history, no-JS by blocking scripts via route abort, modifier-click passthrough, pushState failure by monkeypatching history.pushState to throw), A01-A04 (keyboard: Tab containment inside open dialog, Escape, focus restoration, aria), C01-C04, V01-V04 as specified. Each test prints PASS / FAIL (with measured values) / PENDING (with why it cannot be driven). Run it at desktop 1440x900 and, where the table says so, mobile 390x844, both themes where visual. Save the raw output to _handoff/rw-v2/qa/results.txt.
2. Capture the final atlas: entry, first ThreadScene, Thread Focus (overlay + route), Evidence Focus (index + record), Figure Viewer, conclusion, mobile Contents sheet open; desktop 1440 and mobile 390; light and dark. Save PNGs to docs/blog-work/.../reading-experience-v2/atlas/final/ and write atlas/final/INDEX.md listing each file with one line of what it shows. Keep the directory under 8 MB total (use JPEG quality 80 via Playwright's type="jpeg" if PNGs are too large).
3. Write docs/blog-work/.../reading-experience-v2/CLOSEOUT.md: per-test results table (from results.txt, verbatim statuses; PENDING never becomes PASS); per-package commits with one line each; what was delivered vs DIRECTION V1-V13 (mark each V as done / partial / deferred, with reason); open items carried from $H/07_OPEN_ITEMS.md; deferred items with reasons (include: Opus voice pass on V12 prose and the Tier A fidelity round against composites are deferred from this leg: ICA Claude upstream was down on 2026-09-19; the fidelity round runs as a separate Sol-with-vision leg on the ccx lane and will be appended to this CLOSEOUT by the orchestrator; FIX-LIST §B items not taken); lanes used per leg (taste-fix A3-A9 on Nick's personal Codex gpt-5.6-terra; everything after on ICA gpt-5.6-terra via CC2); the "SAME BYTES EVERYWHERE" PNG caption flag from v2-research-integrated/ASSET-MAP.md; atlas index pointer.

Constraints: no emdashes in anything you write; do not modify src/, do not delete anything in atlas/; no git commands beyond read-only; worktree only.

Final message (under 250 words, write it before you run out of turns): counts of PASS/FAIL/PENDING; every FAIL with its measured value; files written; atlas size.
