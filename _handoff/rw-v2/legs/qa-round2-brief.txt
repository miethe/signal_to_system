Registry Wave reading-experience v2: QA round 2. Your previous attempt (log _handoff/rw-v2/legs/qa-closeout-3.log) could not launch Chromium inside the sandbox; this run has no sandbox, so Chromium launches. Everything from _handoff/rw-v2/legs/qa-closeout-brief.txt still applies (read it first: scope, no image viewing, no src/ edits, no git writes, no emdashes). Existing outputs to iterate on: _handoff/rw-v2/qa/run_qa.py, _handoff/rw-v2/qa/results.txt, docs/blog-work/the-registry-wave-agentic-artifact-supply-chain/reading-experience-v2/CLOSEOUT.md and atlas/final/INDEX.md.

A run of run_qa.py outside the sandbox (_handoff/rw-v2/qa/results-run2.txt) shows: only N01 is actually driven (FAIL: open=True; deltaY=50px; focus=A), N02-N05 are PENDING by design, and everything after dies with `Timeout 1000ms exceeded while waiting for event "page"` (a modifier-click new-tab wait). That is not acceptable: the brief asked for every browser-drivable test to be driven.

Do, in this order:
1. Fix run_qa.py so every test that can be driven in a browser IS driven with a measured PASS/FAIL. Guidance: new-tab waits use context.expect_page() around the click with a 5s timeout, or assert target=_blank/href instead; N02 loops all six beats; N03/N04 drive the nested chain; N05 uses page.go_back()/go_forward() and checks URL + open dialog; N06/N07 open the route URL in a fresh context; N09 aborts *.js routes; N10 monkeypatches history.pushState to throw; A01 tabs through an open dialog and asserts document.activeElement stays inside; A02 emulates reduced motion; A03 sets 320px width and checks no horizontal overflow; C01-C04 and V01-V04 per the $H/05 table. PENDING is allowed only for things a headless browser truly cannot observe (say what).
2. For N01, decide whether deltaY=50px is a defect or the harness measuring before scroll restoration settles (wait for the dialog close event and two animation frames before measuring). Report the truth either way.
3. Run the harness, save output to _handoff/rw-v2/qa/results.txt (overwrite), capture the final atlas as specified (atlas/final/, INDEX.md, under 8 MB).
4. Rewrite CLOSEOUT.md's per-test table from the new results.txt verbatim; keep every other section, updating the atlas pointer and the sentence about Chromium.

Final message under 250 words: PASS/FAIL/PENDING counts; every FAIL with measured value and your one-line diagnosis; atlas size and file count.
