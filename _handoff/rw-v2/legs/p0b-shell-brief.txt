Registry Wave reading-experience v2, package P0-B: static shell + routes (no JS controller yet — that is P0-C).

Read first, in order: docs/blog-work/the-registry-wave-agentic-artifact-supply-chain/reading-experience-v2/DIRECTION.md (V3-V9, V13) and ARCHITECTURE.md (same directory) — ARCHITECTURE.md is binding for route tree (§2), dialog host shape (§3, but do NOT wire the JS controller yet, just build the static content and template scaffolding), data contracts (§4), and component disposition (§5).
$H = /Users/miethe/dev/WIP/agents/theses/registry-wave/reg-wave-review-v4/registry_wave_experience_v2
Read: $H/02_EXPERIENCE_SPEC.md (§4-12), $H/04_COMPONENTS_AND_ASSETS.md, $H/contracts/design-tokens.css, $H/contracts/types.ts.
Read current: src/data/threads.ts, src/data/evidence.ts (both just rewritten, read the actual current file), src/layouts/PostLayout.astro, src/pages/essays/[...slug].astro, src/components/content/{Figure,PostSidebar,ThreadRail,ThreadBeat,ThreadStoryboard,ExecutiveSignal,ClaimBadge,ReceiptDisclosure,EvidenceLegend}.astro, src/styles/global.css.

Build, per ARCHITECTURE.md §2-5:
1. Delete src/components/content/ThreadStoryboard.astro and src/components/content/ThreadBeat.astro (confirmed dead/single-use by the architecture leg; re-grep before deleting to be safe — if either is imported anywhere, stop and report instead of deleting).
2. New src/components/content/ThreadScene.astro: inline per-beat component rendered adjacent to essay prose. Looks up its beat from threads.ts by slug+beatId, renders figure (via Figure.astro, figureId prop wired per ARCHITECTURE.md §5), recap paragraph, kind label, and an "Explore thread" link to /essays/<slug>/thread/<beatId>/. Composition variants (measure/pane/split/inset) per V6 — default measure, pane for dense technical PNGs (the four technical figures: incident-resolution-flow, from-source-to-outcome, drift-across-five-days, run-level-provenance) — accept a `composition` prop.
3. New src/components/content/ThreadDock.astro (replaces ThreadRail.astro's role, ported per ARCHITECTURE §5): compact beat list for the ReadingCompanion sidebar, active-state styling, "All sections" affordance. Delete ThreadRail.astro once ThreadDock supersedes it (re-grep first).
4. New src/components/content/ThreadFocus.astro and src/components/content/EvidenceFocus.astro and src/components/content/FigureViewer.astro: full focus-view renderers per ARCHITECTURE §4 (same component backs both the standalone route and the inline <template> — for THIS leg, just make each render correctly when given props; the <template> wrapping and clone-on-open JS is P0-C's job, but wrap each in <template data-focus-view="..."> per ARCHITECTURE §3 so P0-C has something to clone). FigureViewer needs exact asset, exact caption, long description, real file metadata read from disk at build time (fs.statSync on the public asset path — size, and note you cannot get real dimensions without a PNG library; if none is available in package.json, read width/height from the existing Figure.astro usage or omit dimensions honestly rather than fabricate).
5. New src/components/content/ReadingCompanion.astro (refactor of PostSidebar.astro per ARCHITECTURE §5 / DIRECTION V5): three-slot sticky contract — entry state: nearby outline + ThreadDock + at most one ContextualNote (contextual notes: skip for now if no data source exists, leave the slot empty, do not fabricate); Thread Focus rail: RunningExampleCard + ThreadOverview only (stub these two as simple new components if they don't exist, reading from threads.ts, no fabricated content); Evidence Focus rail: EvidenceGuide (rename/adapt EvidenceLegend.astro) + ReceiptIndex only (new simple component). Keep PostSidebar.astro generic fallback behavior (outline-only) for posts without a thread — check other posts still render (V13).
6. New Astro page files per ARCHITECTURE.md §2 route tree: src/pages/essays/[slug]/thread/[beat].astro, src/pages/essays/[slug]/evidence/index.astro, src/pages/evidence/[record].astro, src/pages/essays/[slug]/figure/[id].astro. Each getStaticPaths per ARCHITECTURE §2. Verify no route collision with existing src/pages/essays/[...slug].astro (ARCHITECTURE risk 1) — if Astro's build fails or misroutes, report exactly what happened rather than guessing a fix blindly; you may adjust file naming/priority to resolve it (Astro static routes take priority over rest params) and note what you changed and why.
7. Wire the six ThreadScene components into src/content/posts/the-registry-wave-agentic-artifact-supply-chain.mdx at the six canonical beat anchors in canonical order (Incident, Deployment, Use, Drift, Detection, Outcome) — do NOT do the full V12 content rewrite (that's a separate Opus leg), just place the ThreadScene components in reasonable locations near matching prose so the shell is testable; leave existing prose otherwise untouched.
8. Light/dark theme: extend src/styles/global.css tokens from $H/contracts/design-tokens.css per V10 (dark matches Tier A midnight/navy/violet/cyan; light gets an equivalent editorial treatment) — use the existing token system, never hardcode dark, keep site fonts.

Constraints: no emdashes. No fabricated data/metrics/dimensions. `npm run check` (no NEW errors beyond the ~2635 baseline in files you did not touch — errors in files you create/edit should be zero) and `npm run build` green before you finish. Do not touch src/data/threads.ts or src/data/evidence.ts content (P0-A leg's job, already done) beyond what's needed to satisfy their existing exported types. Work only in this worktree. No git add -A, no push, no commits (orchestrator commits). End with a report: files created/edited/deleted, build/check status (paste tail), what you verified, what's deferred to P0-C or V12, any route-collision issue found and how resolved.
