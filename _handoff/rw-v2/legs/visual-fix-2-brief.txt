Registry Wave reading-experience v2: visual fidelity round 2. Same rules as round 1 (_handoff/rw-v2/legs/visual-fix-1-brief.txt: read it first; no images, no prose/MDX/data edits, no npm, no git writes, no emdashes, Chromium works, dev server at http://localhost:4399, QA gate 23 PASS / 0 FAIL via run_qa.test_suite, results to _handoff/rw-v2/qa/results-visual-2.txt). Round 1 is committed (5855b3f); the entry now scores 6/10, other regions 2-4/10. Updated review: docs/blog-work/the-registry-wave-agentic-artifact-supply-chain/reading-experience-v2/atlas/final/FIDELITY-REVIEW-sol.md (round 0 kept beside it as FIDELITY-REVIEW-sol-round0.md).

Reading the review: items that say the live viewport "still shows the hero" or "promotes X into a full-page hero" at first-scroll / managed-variation are capture-position artifacts (the screenshot scrolled to a different spot); ignore those. Everything about treatment, sizes, colors, order inside a component is real.

Confirmed by the orchestrator on the entry capture, fix these first:
1. The Executive Signal renders twice at xl: in the rail (correct) AND as a "Why this matters / Carry this forward" purple block in the body under the revision note. Per DIRECTION D7 the body copy is for below xl only; hide it at xl (CSS on the existing split variant; verify no-JS still shows it once below xl).
2. RevisionNote: renders as a heading "Revised September 18, 2026" plus a "· What's changed?" line with a stray leading dot. Make it the compact 38px strip the review describes (date left, "What's changed?" disclosure right, hairline top/bottom), no stray glyph.
3. Rail deeper in the essay: per IMAGE-GUIDE "Author and Executive Signal are normal-flow entry components; deeper in the essay only Reading Companion remains sticky." Verify the collapse-to-pill behavior for the Executive Signal once the companion takes over (ReadingCompanion observer) and that the author card is not sticky.

Then, from the review, in this order: section marker as a horizontal header (4px violet left rule, 40px number tile, hairline), pull quote treatment, ThreadScene / inline comparison plates quieter (navy plate, accent only on icons and status), Figure Viewer as a consistent centered modal with the compact pre-media title block and wide media frame, Thread Focus and Evidence Focus surface polish (header, rail cards), conclusion / WhereThisSits, mobile type scale. Prefer tokens in global.css; dark first, light coherent.

Final message under 250 words: what changed per region (file:lines), QA counts, review items skipped and why.
