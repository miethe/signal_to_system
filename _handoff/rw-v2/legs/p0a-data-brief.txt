Registry Wave reading-experience v2, package P0-A: data + assets (mechanical).

Read first: docs/blog-work/the-registry-wave-agentic-artifact-supply-chain/reading-experience-v2/DIRECTION.md (V2, V3, V8, V9, D4 in Reconciliation table) and ARCHITECTURE.md (§4 data contracts, written by the prior leg) in the same directory.
$H = /Users/miethe/dev/WIP/agents/theses/registry-wave/reg-wave-review-v4/registry_wave_experience_v2
Read: $H/contracts/thread-manifest.json, $H/contracts/evidence-records.json, $H/contracts/types.ts, $H/registry_wave_visual_source_of_truth_2026-09-18/06_SUPPORTING_HANDOFFS/INLINE-FIGURE-IMAGE-USAGE-HANDOFF.md.
Also read: docs/blog-work/the-registry-wave-agentic-artifact-supply-chain/v2-research-integrated/ASSET-MAP.md and REVIEW-V4-DIRECTION.md D9, docs/blog-work/the-registry-wave-agentic-artifact-supply-chain/notes/do-not-disclose.md (binding), and the manuscript src/content/posts/the-registry-wave-agentic-artifact-supply-chain.mdx for the real receipts/evidence already present (redacted YAML, shell output, Aug 6 drift check) and current thread beat text.

Tasks:
1. Rewrite src/data/threads.ts: canonical six-beat order (incident, deployment, use, drift, detection, outcome) per V2. Each ThreadBeat record gets: id, n, label, recap (one real paragraph per beat, grounded in the manuscript, not mockup text), kind ("historical"|"conceptual"|"mechanism"|"proposed" — beat "use" i.e. T03 is "conceptual" per V2's conceptual-branch labeling), establishes: string, doesNotEstablish: string, longDescription: string, image (src/alt/caption using the actual PNG filenames: registry-wave-incident-resolution-flow.png, registry-wave-from-source-to-outcome.png, registry-wave-presence-vs-behavior.png, registry-wave-drift-across-five-days.png, registry-wave-detecting-silent-drift.png, registry-wave-run-level-provenance-infographic.png), relatedEvidenceIds: string[] (not the narrow BeatId[] enum — widen typing per ARCHITECTURE.md risk 2). Keep the Thread/ThreadBeatImage export shape but widen relatedEvidenceIds typing site-wide-safe (string[], not enum).
2. Create src/data/evidence.ts: flat Record<string, EvidenceRecord> seeded from $H/contracts/evidence-records.json but re-grounded: every record must map to a receipt actually present in the manuscript or the notes files. captureType is "redacted capture" ONLY for the two real redacted entries (the redacted YAML, the shell output); everything else is "method summary". Do not fabricate hashes/dates/versions not present in the source. Field set per $H/02_EXPERIENCE_SPEC.md §8 / contracts/types.ts PublicEvidenceRecord, with relatedSceneIds: string[] (not BeatId[]).
3. Delete dead assets per D4 (Reconciliation table) once confirmed unreferenced by any remaining code/content after your threads.ts rewrite: public/assets/posts/the-registry-wave-agentic-artifact-supply-chain/diagram-thread-01-incident.svg through diagram-thread-06-outcome.svg (6 files), diagram-market-wave.svg, diagram-estate-before-after.svg. Grep the whole repo (src/, docs/) for each filename before deleting; if anything still references one, leave it and note why in your final report instead of deleting.
4. Do NOT touch page/route files, components, or PostLayout — that is P0-B/P0-C. Do NOT run `npm run build` changes to other files. You may run `npm run check`/`npm run build` read-only to verify your two data files typecheck, but do not fix unrelated pre-existing errors (baseline ~2600).

Constraints: no emdashes anywhere you write prose. No fabricated evidence, dates, metrics. Mockup/handoff text is reference only, never copied verbatim as final copy — write your own grounded paragraph per beat citing manuscript content. Work only in this worktree, no git add -A, no push. End with a short report: files changed, files deleted (with grep proof), what you verified, what you could not verify.
