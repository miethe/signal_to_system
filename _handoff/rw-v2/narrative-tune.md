# Registry Wave: narrative and presentation pass

Improve `src/content/posts/the-registry-wave-agentic-artifact-supply-chain.mdx`
and its presentation. Optimize for an engaging, technically defensible essay,
not a SkillMeat feature tour or a checklist of prior feedback.

## Start from the current work

Read the repo's current instructions, the essay, its editorial/capture notes,
and `voice/nick-voice-rules.md` plus `nick-hunks.md` under its
`docs/blog-work/` directory. Record HEAD and preserve Nick's working-tree edits.
Do not replace the current article with a stale `drafts/` or `final/` copy.
Treat the accompanying narrative recut as an editorial candidate, not approved copy.

## Narrative direction

Keep the stale reviewer as a short opening symptom. Center the story on the
stronger dilemma: some deployments need updates; others contain useful local
changes that a blanket update would erase. Keep separate incidents separate.

Show the positive value: shared improvements can travel, deliberate project
variation can survive, and local learning can become a candidate for wider reuse.
Retain "trusted artifact is not automatically trusted use" as a boundary,
without retelling the contract and deterministic-control essays.

Use one brief illustrative Source → Collection → Project example. Distinguish
intended binding, deployed content, and observed runtime loading. An older pinned
version is not automatically stale; a local difference is not automatically approved.
Keep the main argument understandable with every disclosure closed.

## Delegate bounded work

Use current routing policy and ICA for eligible evidence, capture, and implementation
tasks with disjoint file ownership. Keep orchestration, final judgment, and restricted
work on their required lanes. Resolve model aliases from current policy, not this prompt.

At most one opt-in frontier consultation for story selection: compare three framings,
recommend one, return an opening and section map within 1,200 words. No recursive
delegation or open-ended rewrite loop.

## Evidence and captures

Find the existing screenshot directions and primary workflow-divergence records.
Use the existing SkillMeat instance through its approved access path. Start read-only.
Demonstrate stale deployment, protected local divergence, and a deliberate update
decision in an authorized isolated demo workspace. No blanket sync, force overwrite,
policy changes, or mutation of real consuming projects.

Use genuine screenshots or clearly labeled reconstructions. Identify each mechanism
as SkillMeat-native, local AOS tooling, or proposed integration. Do not invent UI,
historical results, runtime loading, or effectiveness metrics. Missing native
functionality becomes a gap, not a reason to expand the product scope.

## Presentation

Place a new illustrative hero before the title, with separate hero/social/technical
assets. Replace the old Figure 01 caption/art mismatch. Use one compact three-beat
evidence panel with optional technical detail; no autoplay or competing persistent
sidebars. Choose one primary taxonomy/architecture representation instead of duplicating them.

Add accessible figure enlargement, responsive images, reduced-motion behavior,
and static/no-JavaScript fallbacks. Trace any new fields through schema, route,
layout, and SEO. Keep broader homepage/series polish in a separate patch.

## Deliver

Return the revised essay diff, evidence ledger, sanitized captures, visual prompts,
page patch, and test results. Verify citations, voice, mobile layout, keyboard modal
behavior, diagram/caption agreement, and unsupported claims. Report unavailable
evidence plainly. Do not publish, merge, deploy, or overwrite Nick's edits without approval.