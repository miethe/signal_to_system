# Synthetic Labs fixtures
All content in this directory is synthetic only.
Fixtures load only in development or with `S2S_LABS_FIXTURES=1`.
`queue-latency` exercises an approved report state.
`annotation-guidelines` exercises a minimal no-conclusion state.
`withdrawn` exercises the withdrawal notice state.
`gate-rejected` exercises a refused candidate release.
These records never describe real research or systems.
