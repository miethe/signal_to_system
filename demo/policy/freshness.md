# Checklist: Freshness

A demo is fresh when:

- screenshots were generated from the current app state
- capture script passes
- narration matches visible UI
- product names are current
- feature flags match intended demo state
- app version or commit hash is recorded
- stale outputs are marked or removed
